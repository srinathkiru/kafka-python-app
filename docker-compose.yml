version: '3.8'

services:
  producer:
    build: ./producer
    depends_on:
      - kafka
    restart: on-failure      # Retry if Kafka isn't ready yet

  consumer:
    build: ./consumer
    depends_on:
      - kafka
    restart: on-failure